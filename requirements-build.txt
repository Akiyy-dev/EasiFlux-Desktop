# Build-time dependency snapshot for Windows packaging pipelines.
# Source of truth remains pyproject.toml.

EasiFlux-SDK[websocket]>=0.2.0,<1.0
PySide6>=6.6
qasync>=0.27
keyring>=25.0
cryptography>=42.0
pyqtgraph>=0.13
platformdirs>=4.0
pyinstaller>=6.0
